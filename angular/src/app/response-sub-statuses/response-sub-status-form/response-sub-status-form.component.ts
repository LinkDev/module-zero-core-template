import { Component, ViewChild, Injector, Output, EventEmitter, ElementRef, OnInit, AfterViewInit,ViewChildren,QueryList } from '@angular/core';
import { ModalDirective } from 'ngx-bootstrap';
import * as moment from 'moment';
import { ResponseSubStatusServiceProxy, ResponseSubStatusDto, PagedResultDtoOfResponseSubStatusDto } from '@shared/service-proxies/service-proxies';
import { FormComponentBase } from '@shared/form-component-base';
import * as _ from "lodash";

@Component({
  selector: 'response-sub-status-form-modal',
  templateUrl: './response-sub-status-form.component.html'
})
export class ResponseSubStatusFormComponent extends FormComponentBase<ResponseSubStatusDto> implements OnInit, AfterViewInit {
	
    constructor(
        injector: Injector,
			private _responseSubStatusService: ResponseSubStatusServiceProxy    ) {
        super(injector);
    }

	ngOnInit() {

    }

	show(id?:string): void {
        if (!id) {
            this.active = true;
            this.modal.show();
            this.item = new ResponseSubStatusDto();
			this.item.id='00000000-0000-0000-0000-000000000000';
            this.isNew = true;
        }
        else {
            this._responseSubStatusService.get(id)
                .subscribe(
                (result) => {
                    this.item = result;
                    this.active = true;
                    this.modal.show();
                });
            this.isNew = false;
        }
    }

	save(): void {
        console.log(this.item);
        this.saving = true;

        if (this.isNew) {
            this._responseSubStatusService.create(this.item)
                .finally(() => { this.saving = false; })
                .subscribe(() => {
                    this.notify.info(this.l('Saved Successfully'));
                    this.close();
                    this.modalSave.emit(null);
                });
        }
        else {
            this._responseSubStatusService.update(this.item)
                .finally(() => { this.saving = false; })
                .subscribe(() => {
                    this.notify.info(this.l('Saved Successfully'));
                    this.close();
                    this.modalSave.emit(null);
                });
        }
    }
}